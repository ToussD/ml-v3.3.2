# Challenge — version conda
# Même mini projet ML (analyse de churn clients).
name: challenge-conda
channels:
  - conda-forge
dependencies:
  - python=3.11
  - scikit-learn=1.8
  - pandas=3.0
  - numpy=2.4
  - matplotlib=3.10
  - joblib=1.5
